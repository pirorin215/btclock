# btclock TODO

最終整理: 2026-09-26(cycleclock v0.3.0 運用開始 — 振動ベーススリープ・バッテリー監視まで実機確認済み)

## 残課題

### 到着物待ち(パーツ別)
- [ ] **BW版ePaper 到着時の差替**(aliexpress注文中) — ファーム変更不要(GxEPD2_213_B74は元々BW向け)。フル更新が~2-3秒に高速化。現在は手持ち3色パネルで低速描画
- [ ] **System OFF 電流の実測**(μAテスタ到着後) — 給電経路にμAレンジ挿入(ソーラー化後は「電池→TP4056モジュールB+間」で測ると全体像が取れる)。タクト(D0)長押し2秒でSystem OFF→測定。目安一桁μA(フォーラム実測2.4μA+TP4056夜間逆流~2-5μA)。数百μAなら「正しくSystem OFFに入っていない」(USB残留・コード不備の疑い)。あわせて電圧校正: テスタ実測と GET:battery 表示を突合し、ズレれば `BATT_DIV_MULT`(現状2.96)を調整
- [ ] **ソーラー補助充電の組込**(TP4056保護付き6パッド版・注文中) — 構成・配線は `~/www/kento/xiao-ble-solar-tp4056.html` 確定済み: パネル5V→IN+ / 18650→B+ / XIAO電池パッド→OUT+(3.0-4.2V)。電圧監視・低電圧警告(3.5V)は変更不要でOUT+経由でそのまま動く。到着時確認: ①JST極性 ②小容量パネルならRprogを1.2k→5kΩ(1A→240mA) ③夜間のパネル逆流(気になればIN+にショットキー直列)

### v0.2(SW-18020P 到着後・10月初旬)
- [ ] 振動センサー本組込 — タクトスイッチと差し替え(向き・感度の実機調整含む)。ファーム側(v0.3.0の振動ベーススリープ)は導通パルスをそのまま扱うため変更不要
- [ ] 誤起床ガード — 起床後2〜3秒センサーをポーリングし断続チャタリングが続くか確認、続かなければ即System OFFへ戻る(風・通行車両対策)
- [ ] 低電圧通知のePaper表示 — 「要充電」描画(閾値3.5V以下)。電圧取得(v0.2.2)は済み、残りは描画と閾値設計のみ。ソーラー化後は3.5V警告→DW01の2.4V遮断の二段構えになる

### 運用観察(進行中)
- [ ] 様子見 — LEDの明るさ感覚(`LED_DIM_PWM_VALUE`/`LED_PULSE_MS` で即調整可)・ヘッダー電圧の推移・3.5V赤字表示のタイミング。不具合時の切り分けはArduino IDEシリアルモニタで(consolelog.shはDTRが立たず何も出ない既知問題)

## 完了済み

### 2026-09-26
- [x] **振動ベーススリープ**(v0.3.0・実機動作確認済み) — 「最終振動(スイッチ導通)から3分」でBLE接続中でも切断してSystem OFF。スマホが近くにいても確実に眠る(自宅・近距離座り込み問題を解決)。実機で「3分でスリープ画面→System OFF」を確認
- [x] **LEDの低消費化**(v0.3.0) — 常時点灯系は超低デューティPWM(約1.2%)、点滅系は2秒に1回50msの短パルス(自作キーボード方式・エラー時500ms間隔)
- [x] **バッテリー電圧の取得・アプリ集計**(v0.2.2で動作確認済み) — 内蔵分圧(P0.31/P0.14)で60秒キャッシュ測定+起動直後即測定、`GET:battery` プロトコル。アプリは接続直後+5分ごとに取得しDataStoreに時系列保存(最大10000件)・ヘッダーに最新電圧表示。実機で取得→保存→表示まで確認(USB充電中4.09V/バッテリ運用3.77V)。修正履歴: ①起動直後の初回取得失敗→即測定化 ②固定長32B特性の応答に残バイトが混入し解析が無音失敗→ファーム側ゼロクリア+アプリ側正規表現解析
- [x] **BTClockMob 新APK適用**(ワイヤレスデバッグで修正版を適用済み) — 複数デバイス対応+バッテリー集計込み
- [x] **実機組立・動作確認** — 書込・ePaper表示・Androidペアリング・時刻同期が一発で動作
- [x] **スリープ時スナップショット表示**(v0.1.5) — System OFF直前に「日付+曜日/乗車時間/走行区間」を描画しゼロ電力で保持。停車中に時刻と勘違いさせない(ESP32版でモーション学習が必要だった課題が、スリープ=ソフトウェアイベント化により自明に解決)
- [x] **System OFF手動テストの安定化**(v0.1.4) — 押下中のDETECT即成立による即時起床(見かけ上の再起動)を回避: スイッチ解放待ち+起床要因ログ(RESETREAS.OFF)追加

### 2026-09-25
- [x] **cycleclock v0.1.0 実装** — 新規 `cycleclock/`: XIAO BLE + WeAct 2.13" ePaper + System OFF振動(タクト)ウェイク。bikeclock(XIAO・BLE/時刻処理)とbikeclock_esp32(ePaper描画)から移植
- [x] **BTClockMob 複数デバイス対応** — 接続先未選択時、ペアリング済みBikeClock全台にスキャンフィルタを掛け実際に広告しているデバイスへ自動接続(バイク/自転車の切替がアプリ操作不要に)。選択時は従来どおり1台限定(後方互換)
- [x] ボード選定: **XIAO BLE (nRF52840)** — System OFF実測2.4μA(管理された計測)・fastrec2の電池運用実績・BLE実績はbikeclockにあり
- [x] 電源方針: 18650をバッテリー端子へ直結 — 給電源が存在しないため内蔵電池で自立。充電/交換のメンテは低電圧通知で運用
- [x] 動作モデル: 乗車時のみ稼働 — バイク版(キーオン→コールドスタート→BLE同期・8年ノーメンテ実績)と同じ寿命モデルを振動ウェイクで再現
- [x] 5V UPSブースト基板(Walfront・18650用)は不採用 — 5Vを必要とする部品がない・二重変換が無駄・5V給電ではXIAO BLEのスリープ電流が下がらない実測あり
- [x] 電動アシスト自転車の走行用バッテリーからは給電しない — 保証・バッテリー消耗の懸念
- [x] ハブダイナモ非搭載のため給電系(発電)案は消滅
- [x] 給電はマイコンボードのバッテリーパッド直結 — XIAO BLE・ESP32-S3 SuperMini とも背面にBAT+/BAT-端子+オンボード充電器あり

## 概要

自転車搭載版の新規製作。バイク版と同様の「乗車時のみ動作」(スマホBLE同期・非乗車時はほぼゼロ消費)を、キーオン電源の代わりにバッテリー直結+振動ウェイクで実現する。ファームウェアは `cycleclock/`(v0.3.0)。設計思想の経緯は DEVELOPMENT_BACKGROUND.md 参照。
